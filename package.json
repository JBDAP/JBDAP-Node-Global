{
  "name": "jbdap-global",
  "version": "0.1.6",
  "description": "Prepare a better global environment for nodejs.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "pretest": "yarn run build",
    "build": "babel src -d lib",
    "prepare": "yarn run build",
    "updependencies": "yarn upgrade jbdap-nice-error@latest lodash@latest",
    "preupdependencies": "yarn upgrade @babel/cli@latest @babel/core@latest @babel/preset-env@latest jest@latest"
  },
  "author": "JBDAP TEAM <jbdapteam@outlook.com>",
  "license": "GPL-3.0",
  "dependencies": {
    "jbdap-makeup": "^0.1.3",
    "jbdap-nice-error": "^0.1.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "jest": "^24.8.0"
  }
}
